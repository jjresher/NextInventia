SUPABASE_URL=
#anon key. La usa el backend en producción (dependencies.py) — solo necesita SELECT.
SUPABASE_ANON_KEY=
# service_role key. Bypassa RLS por diseño. Solo para los scripts de escritura en
# backend/exel/ (upload_to_supabase.py, generate_embeddings.py, cluster_patentes.py),
# que corren en desarrollo, nunca en el backend de producción. Distinta de SUPABASE_ANON_KEY.
SUPABASE_SERVICE_ROLE_KEY=
FRONTEND_ORIGIN=http://localhost:3000
GEMINI_API_KEY=
